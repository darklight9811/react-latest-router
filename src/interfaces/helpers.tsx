export interface iPath {
    raw : string,
    data: Object,
}

export interface iGuardData {
    route                   : Object,
    router                  : Object,
    context                 : Object,
}